[package]
name = "action-space"
version = "0.1.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
core-types = { path = "../core-types" }
position = { path = "../position" }
rules = { path = "../rules" }
